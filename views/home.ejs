<%- include('partials/header'); -%>

<h1>Home</h1>

<p><%= startingContent %></p>

 <% posts.forEach(function(post){%>

       <h1> <%= post.title %> </h1> 
       <p> <%= post.content %> </p> 
    
     <% }) %>
    

<%- include('partials/footer'); -%>

<!-- 
// Placing the slash in front (/partials/footer) will tell the server you're trying to find a file from your root directory.

// __dirname is not available in your templates, and writing the full path from root will give you unexpected results when you move the app over. -->

<!-- for(var i=0; i<posts.lenght; i++){ or use this loop instead of foreach --> 


